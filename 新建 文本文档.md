Dictionary<Key,Value>
KV存储，具体读的时候要写读的是k还是v，
例如item.Value
item其实包含了k和v，需要具体再访问value才是真正的对象



**`Dictionary` 不是线程安全的，`ConcurrentDictionary` 是线程安全的**。

|            | `Dictionary` | `ConcurrentDictionary`       |
| :--------- | :----------- | ---------------------------- |
| 多线程写入 | ❌ 会崩       | ✅ 安全                       |
| 遍历时删除 | ❌ 抛异常     | ✅ 安全                       |
| 性能       | 快           | 稍慢（有锁开销，但做了优化） |
| 适用场景   | 单线程       | 多线程并发读写               |




检测心跳不能使用Socket.Connected，因为这个玩意只是记录了上一次该实例是否连接到远程主机，并不是真正的实时状态
需要用Socket.Poll方法







## 目前消息接收
```
网络收到 "你好"
    │
    ▼
Server.ReceiveLoop 线程
    │ socket.Receive() 返回数据
    │ 转成 string "你好"
    │
    ▼
MessageReceived?.Invoke("192.168.1.5:12345", "你好")
    │        └── 遍历所有订阅者，逐个调用
    │
    ▼
Chat.cs 的 lambda 被调用
    │
    ▼
OnMessageReceived?.Invoke("192.168.1.5:12345", "你好")
    │
    ▼
Form1.cs 的 lambda 被调用
    │ (发现不在 UI 线程，用 Invoke 切回去)
    ▼
chatList.AddToBottom(TextChatItem)   → 气泡出现在界面上
```